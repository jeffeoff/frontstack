{
  "name": "frontstack",
  "description": "Simple front-end SASS compilation and JS bundling.",
  "author": "Jeff Eoff",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "main": "gulp watch",
  "devDependencies": {
    "@parcel/transformer-sass": "2.11.0",
    "jquery": "^3.7.1",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.9.1",
    "parcel-resolver-ignore": "^2.1.5",
    "prettier": "^2.8.3",
    "sass": "^1.62.1"
  },
  "engines": {
    "node": ">= 18"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "format": "run-p format:*",
    "format:js": "npx prettier --write js/",
    "format:sass": "npx prettier --write sass/",
    "sass": "sass scss/main.scss dist/css/main.css",
    "watch": "parcel watch --no-hmr --no-autoinstall",
    "build": "parcel build --no-optimize",
    "production": "parcel build --no-source-maps --detailed-report"
  },
  "eslintConfig": {
    "env": {
      "es6": true
    }
  },
  "targets": {
    "css": {
      "source": "./scss/main.scss"
    },
    "js": {
      "source": "./js/main.js"
    },
    "default": {
      "distDir": "./dist"
    }
  },
  "parcelIgnore": [
    "dist/*.*",
    "js/vendor/*.*"
  ]
}
